# Зависимости (ставятся через npm install)
node_modules/

# Кэши сборки
.vite/
*.tsbuildinfo

# Логи
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Редакторы и ОС
.vscode/
.idea/
.DS_Store
Thumbs.db

# ВАЖНО: dist/ НЕ игнорируется — собранная игра под контролем версий
# (деплой = git pull + отдать папку dist статическим сервером).
